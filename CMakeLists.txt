cmake_minimum_required(VERSION 3.22)

project(StaticCathedral VERSION 0.1.0 LANGUAGES C CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

option(STATICCATHEDRAL_BUILD_PLUGIN "Build the JUCE plug-in targets" ON)
option(STATICCATHEDRAL_BUILD_TESTS "Build the test targets" ON)
set(STATICCATHEDRAL_ARTIFACT_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/artifacts" CACHE PATH "Stable product staging root")
set(EHL_JUCE_SOURCE_DIR "" CACHE PATH "Existing JUCE source checkout; avoids network when set")

add_library(staticcathedral_dsp STATIC
    Source/dsp/FoundationDSP.cpp)
target_include_directories(staticcathedral_dsp PUBLIC Source)
target_compile_features(staticcathedral_dsp PUBLIC cxx_std_17)
if(MSVC)
    target_compile_options(staticcathedral_dsp PRIVATE /W4)
else()
    target_compile_options(staticcathedral_dsp PRIVATE -Wall -Wextra -Wpedantic)
endif()

if(STATICCATHEDRAL_BUILD_TESTS)
    enable_testing()
    add_executable(staticcathedral_dsp_tests Tests/DSPTests.cpp)
    target_link_libraries(staticcathedral_dsp_tests PRIVATE staticcathedral_dsp)
    add_test(NAME staticcathedral_dsp_tests COMMAND staticcathedral_dsp_tests)
endif()

if(STATICCATHEDRAL_BUILD_PLUGIN)
    include(FetchContent)
    if(EHL_JUCE_SOURCE_DIR)
        if(NOT EXISTS "${EHL_JUCE_SOURCE_DIR}/CMakeLists.txt")
            message(FATAL_ERROR "EHL_JUCE_SOURCE_DIR does not contain JUCE: ${EHL_JUCE_SOURCE_DIR}")
        endif()
        add_subdirectory("${EHL_JUCE_SOURCE_DIR}" "${CMAKE_CURRENT_BINARY_DIR}/juce")
    elseif(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/JUCE/CMakeLists.txt")
        add_subdirectory(JUCE)
    else()
        FetchContent_Declare(
            JUCE
            GIT_REPOSITORY https://github.com/juce-framework/JUCE.git
            GIT_TAG 91ad83ae34a81e0833b1a2b0866f54846370ae53
            GIT_SHALLOW FALSE)
        FetchContent_MakeAvailable(JUCE)
    endif()
    add_subdirectory(modules/juce-ehl-design-module)

    set(STATICCATHEDRAL_FORMATS VST3 Standalone)
    if(APPLE)
        list(APPEND STATICCATHEDRAL_FORMATS AU)
    endif()

    juce_add_plugin(StaticCathedral
        VERSION ${PROJECT_VERSION}
        COMPANY_NAME "EsionHsrahLatigid"
        BUNDLE_ID "jp.ehl.staticcathedral"
        PRODUCT_NAME "StaticCathedral"
        DESCRIPTION "eight-line harsh-noise FDN reverb"
        PLUGIN_MANUFACTURER_CODE EHL_
        PLUGIN_CODE StCt
        FORMATS ${STATICCATHEDRAL_FORMATS}
        IS_SYNTH FALSE
        NEEDS_MIDI_INPUT FALSE
        NEEDS_MIDI_OUTPUT FALSE
        IS_MIDI_EFFECT FALSE
        EDITOR_WANTS_KEYBOARD_FOCUS TRUE
        MICROPHONE_PERMISSION_ENABLED TRUE
        MICROPHONE_PERMISSION_TEXT "StaticCathedral processes live audio input."
        COPY_PLUGIN_AFTER_BUILD FALSE)

    target_sources(StaticCathedral PRIVATE
        Source/ParameterIDs.h
        Source/PluginEditor.cpp
        Source/PluginEditor.h
        Source/PluginProcessor.cpp
        Source/PluginProcessor.h)
    target_include_directories(StaticCathedral PRIVATE Source)
    target_compile_definitions(StaticCathedral PUBLIC
        JUCE_REPORT_APP_USAGE=0
        JUCE_USE_CURL=0
        JUCE_VST3_CAN_REPLACE_VST2=0
        JUCE_WEB_BROWSER=0)
    target_link_libraries(StaticCathedral
        PRIVATE
            staticcathedral_dsp
            EHL::JuceDesign
            juce::juce_audio_utils
            juce::juce_dsp
        PUBLIC
            juce::juce_recommended_config_flags
            juce::juce_recommended_lto_flags
            juce::juce_recommended_warning_flags)

    target_sources(StaticCathedral_Standalone PRIVATE Source/StandaloneApp.cpp)
    target_compile_definitions(StaticCathedral_Standalone PRIVATE JUCE_USE_CUSTOM_PLUGIN_STANDALONE_APP=1)

    set(ehl_stage_platform "linux-x64")
    if(APPLE)
        set(ehl_stage_platform "macos-arm64")
    elseif(WIN32)
        set(ehl_stage_platform "windows-x64")
    endif()
    set(ehl_juce_products "${CMAKE_CURRENT_BINARY_DIR}/StaticCathedral_artefacts/$<CONFIG>")
    set(ehl_staged_products "${STATICCATHEDRAL_ARTIFACT_ROOT}/plugin-release/${ehl_stage_platform}")

    set(ehl_stage_dependencies StaticCathedral_VST3 StaticCathedral_Standalone)
    set(ehl_stage_commands
        COMMAND ${CMAKE_COMMAND} -E make_directory "${ehl_staged_products}/vst3"
        COMMAND ${CMAKE_COMMAND} -E make_directory "${ehl_staged_products}/standalone"
        COMMAND ${CMAKE_COMMAND} -E make_directory "${ehl_staged_products}/au")

    if(APPLE)
        list(APPEND ehl_stage_commands
            COMMAND ${CMAKE_COMMAND} -E rm -rf "${ehl_staged_products}/vst3/staticcathedral_vst3_plugin.vst3"
            COMMAND ${CMAKE_COMMAND} -E rm -rf "${ehl_staged_products}/standalone/staticcathedral_standalone_plugin.app"
            COMMAND ${CMAKE_COMMAND} -E rm -rf "${ehl_staged_products}/au/staticcathedral_au_plugin.component"
            COMMAND ${CMAKE_COMMAND} -E copy_directory "${ehl_juce_products}/VST3/StaticCathedral.vst3" "${ehl_staged_products}/vst3/staticcathedral_vst3_plugin.vst3"
            COMMAND ${CMAKE_COMMAND} -E copy_directory "${ehl_juce_products}/Standalone/StaticCathedral.app" "${ehl_staged_products}/standalone/staticcathedral_standalone_plugin.app"
            COMMAND ${CMAKE_COMMAND} -E copy_directory "${ehl_juce_products}/AU/StaticCathedral.component" "${ehl_staged_products}/au/staticcathedral_au_plugin.component"
            COMMAND codesign --force --deep --sign - "${ehl_staged_products}/vst3/staticcathedral_vst3_plugin.vst3"
            COMMAND codesign --force --deep --sign - "${ehl_staged_products}/standalone/staticcathedral_standalone_plugin.app"
            COMMAND codesign --force --deep --sign - "${ehl_staged_products}/au/staticcathedral_au_plugin.component")
        list(APPEND ehl_stage_dependencies StaticCathedral_AU)
    elseif(WIN32)
        list(APPEND ehl_stage_commands
            COMMAND ${CMAKE_COMMAND} -E rm -rf "${ehl_staged_products}/vst3/staticcathedral_vst3_plugin.vst3"
            COMMAND ${CMAKE_COMMAND} -E copy_directory "${ehl_juce_products}/VST3/StaticCathedral.vst3" "${ehl_staged_products}/vst3/staticcathedral_vst3_plugin.vst3"
            COMMAND ${CMAKE_COMMAND} -E copy "${ehl_juce_products}/Standalone/StaticCathedral.exe" "${ehl_staged_products}/standalone/staticcathedral_standalone_plugin.exe")
    else()
        list(APPEND ehl_stage_commands
            COMMAND ${CMAKE_COMMAND} -E copy_directory "${ehl_juce_products}/VST3/StaticCathedral.vst3" "${ehl_staged_products}/vst3/staticcathedral_vst3_plugin.vst3"
            COMMAND ${CMAKE_COMMAND} -E copy "${ehl_juce_products}/Standalone/StaticCathedral" "${ehl_staged_products}/standalone/staticcathedral_standalone_plugin")
    endif()

    list(APPEND ehl_stage_commands
        COMMAND ${CMAKE_COMMAND}
            -DPRODUCT_NAME=StaticCathedral
            -DSLUG=staticcathedral
            -DBUNDLE_ID=jp.ehl.staticcathedral
            -DPLUGIN_CODE=StCt
            -DSTAGE_DIR=${ehl_staged_products}
            -P "${CMAKE_CURRENT_SOURCE_DIR}/cmake/WriteArtifactsManifest.cmake")

    add_custom_target(ehl_stage_products
        ${ehl_stage_commands}
        DEPENDS ${ehl_stage_dependencies}
        COMMENT "Staging StaticCathedral products under ${STATICCATHEDRAL_ARTIFACT_ROOT}/plugin-release")

    if(STATICCATHEDRAL_BUILD_TESTS)
        add_executable(staticcathedral_plugin_tests Tests/PluginTests.cpp)
        target_include_directories(staticcathedral_plugin_tests PRIVATE Source)
        target_link_libraries(staticcathedral_plugin_tests PRIVATE StaticCathedral juce::juce_audio_utils)
        add_test(NAME staticcathedral_plugin_tests COMMAND staticcathedral_plugin_tests)

        add_executable(staticcathedral_editor_tests Tests/EditorTests.cpp)
        target_include_directories(staticcathedral_editor_tests PRIVATE Source)
        target_link_libraries(staticcathedral_editor_tests PRIVATE StaticCathedral EHL::JuceDesign juce::juce_audio_utils)
        add_test(NAME staticcathedral_editor_tests COMMAND staticcathedral_editor_tests)

        add_test(
            NAME staticcathedral_artifact_contract
            COMMAND ${CMAKE_COMMAND}
                -DSTAGE_DIR=${ehl_staged_products}
                -DSLUG=staticcathedral
                -DEXPECT_AU=$<BOOL:${APPLE}>
                -P "${CMAKE_CURRENT_SOURCE_DIR}/Tests/VerifyArtifacts.cmake")
    endif()
endif()
