name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    uses: EsionHsrahLatigid/yup-actions/.github/workflows/plugin-release.yml@d7746cba582c0a00e6e7f0cdc37ae3b2d38830d8
    permissions:
      actions: read
      contents: write
    with:
      product_name: StaticCathedral
